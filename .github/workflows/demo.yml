name: multiple event triggers

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build-on-linux:
    runs-on: ubuntu-latest

    steps:
      - name: greeting
        run: echo "Hello world"
