name: use github action context

on: workflow_dispatch

jobs:
  build-on-linux:
    runs-on: ubuntu-latest

    steps:
      - name: show repository URL
        uses: echo ${{ gitHub.repositoryURL }}
